'use client'

import First from "../components/first";

export default function HomePage() {
  return (
    <main>
      <div className="w-full h-screen">
        <First/>
      </div>
    </main>
  );
}
